# Production environment configuration
# When running in production mode (./start.sh --prod), the frontend is served
# from the same server as the backend, so we use relative paths for API calls.

# Leave VITE_API_URL empty to use relative paths (same origin)
VITE_API_URL=
